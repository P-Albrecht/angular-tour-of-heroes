{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "msedge",
      "request": "launch",
      "name": "Start app headless",
      "url": "http://localhost:4200",
      "runtimeArgs": ["--headless"],
      "preLaunchTask": "npm: start",
      "webRoot": "${workspaceFolder}"
    },
    {
      "type": "msedge",
      "request": "launch",
      "name": "Start app and launch Edge",
      "url": "http://localhost:4200",
      "preLaunchTask": "npm: start",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
